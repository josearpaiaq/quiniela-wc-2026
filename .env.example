# Neon Postgres connection string (José la crea en neon.tech)
DATABASE_URL=""

# Secret for signing JWTs (generate with: openssl rand -base64 32)
JWT_SECRET=""

# This email becomes admin on registration
ADMIN_EMAIL=""
