want to add a recover password feature to the app. taking in count that the email is not used for recieving actual emails, we need a way to recover the password without the user having to get an email.

also, we need to add an eye to the input password element, maybe this could be a component that we can reuse in other parts of the app.